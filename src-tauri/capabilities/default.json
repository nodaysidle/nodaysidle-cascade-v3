{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window permissions for packet generation and folder selection.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
