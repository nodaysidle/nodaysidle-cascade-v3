import type { SemanticBlueprint } from "../../src/schema"

export const nodaysidleVoiceV5Blueprint = {
  productName: "NODAYSIDLE Voice",
  summary: "A native macOS menu-bar speech-to-text app for global-hotkey dictation, cloud transcription, optional text refinement, paste-back, local history, reusable modes, custom vocabulary, and privacy-focused local storage.",
  targetUsers: [
    "Professionals who dictate into email, chat, documents, and code editors",
    "Users with repetitive strain or mobility limitations who prefer voice input",
    "Multilingual and privacy-conscious macOS power users",
  ],
  goals: [
    "Dictate into any macOS text field from a global hotkey with minimal delay",
    "Provide accurate transcription with optional cleanup, formatting, translation, or rewriting",
    "Insert only completed text at the previous cursor position without losing clipboard content",
    "Keep history, modes, vocabulary, and settings local while protecting credentials",
    "Recover from provider and insertion failures without losing completed transcripts",
  ],
  nonGoals: [
    "Offline on-device speech recognition",
    "Cross-device synchronization",
    "Video or screen recording",
    "Team collaboration",
    "Regulated transcription guarantees",
  ],
  features: [
    {
      name: "Global hotkey dictation",
      userOutcome: "Start and stop dictation from any application without opening the app window.",
      trigger: "Press a configured global hotkey in push-to-talk or start-stop mode.",
      behavior: "Capture microphone audio, show the floating HUD, route audio to the selected transcription provider, and insert or copy only the completed final text according to the paste setting.",
      failureOutcome: "Show a classified error, retain only recoverable input, and permit an explicit retry or provider change.",
      acceptanceSignals: ["Push-to-talk stops on release", "Start-stop mode stops on the second action", "No partial or failed text is inserted"],
    },
    {
      name: "Floating recording HUD",
      userOutcome: "See recording status, duration, input level, and terminal feedback at a glance.",
      trigger: "Recording starts from a hotkey or menu-bar action.",
      behavior: "Display a compact floating HUD with microphone level, elapsed time, current state, cancellation, and readable success or error feedback.",
      failureOutcome: "Show the specific permission or device failure and never leave invisible capture active.",
      acceptanceSignals: ["HUD appears when recording starts", "Cancel stops capture without insertion", "HUD closes after every terminal path"],
    },
    {
      name: "Provider and model configuration",
      userOutcome: "Choose independent transcription and refinement roles and test their configuration.",
      trigger: "Open provider settings or recover from a provider failure.",
      behavior: "Offer Deepgram for live transcription and OpenRouter for finalized-audio transcription and optional text refinement, with independent model selection and explicit connection testing.",
      failureOutcome: "Keep the prior valid selection and report a privacy-safe actionable category without making a paid request.",
      acceptanceSignals: ["Provider choices persist", "Transcription and refinement models are independent", "Connection failures are actionable"],
    },
    {
      name: "Text refinement and dictation modes",
      userOutcome: "Receive text polished according to the selected mode without changing its meaning.",
      trigger: "Select a built-in or custom mode before or during dictation.",
      behavior: "Optionally refine the raw transcript for punctuation, formatting, tone, vocabulary, or output language while preserving the raw transcript and supporting a no-change raw mode.",
      failureOutcome: "Preserve the raw transcript and allow refinement retry or use of the raw text.",
      acceptanceSignals: ["Raw mode returns the transcript unchanged", "Refinement failure keeps raw text", "Custom modes apply only to future operations"],
    },
    {
      name: "Paste-back and clipboard handling",
      userOutcome: "Insert completed text at the prior editable target or keep it available for manual insertion.",
      trigger: "A complete successful transcript is ready and a paste behavior is configured.",
      behavior: "Restore the previously focused application and editable target, prefer direct accessibility insertion, and use a clipboard-based paste only when direct insertion is unavailable.",
      failureOutcome: "Preserve the completed transcript, prior clipboard representations, and any later external clipboard mutation while reporting insertion failure.",
      acceptanceSignals: ["Direct insertion does not touch the clipboard", "Fallback preserves clipboard data", "External clipboard changes are never overwritten", "Partial or failed text is never pasted"],
    },
    {
      name: "Local transcription history",
      userOutcome: "Search, reuse, edit, favorite, or delete past completed transcriptions.",
      trigger: "Open history or complete an accepted transcription while history is enabled.",
      behavior: "Store searchable local history with source application, duration, provider, mode, raw text, refined text, outcome, usage, and explicit retention controls.",
      failureOutcome: "Keep the completed transcript visible for immediate copy and never claim an unsuccessful write was saved.",
      acceptanceSignals: ["Search works offline", "Deletion is atomic", "Disabled history creates no new records", "Retention settings are enforced"],
    },
    {
      name: "Custom vocabulary and replacement rules",
      userOutcome: "Keep names, technical terms, acronyms, and preferred replacements consistent.",
      trigger: "Add, edit, reorder, or delete a vocabulary rule.",
      behavior: "Maintain a local ordered vocabulary with deterministic conflict detection and apply valid changes only to future transcription or refinement operations.",
      failureOutcome: "Keep invalid or conflicting values editable without changing the saved collection.",
      acceptanceSignals: ["Vocabulary changes persist", "Conflicts are reported", "Deleting a rule does not corrupt other rules"],
    },
    {
      name: "Permission guidance and recovery",
      userOutcome: "Resolve microphone, Accessibility, or Input Monitoring denial without guesswork.",
      trigger: "Startup or an explicit action detects a missing permission.",
      behavior: "Show permission-specific guidance, open the relevant System Settings location when possible, and recheck authorization after explicit user action.",
      failureOutcome: "Explain unavailable features, keep unaffected behavior usable, and avoid repeated disruptive prompts.",
      acceptanceSignals: ["Denied microphone blocks capture", "Denied Accessibility keeps a manual path", "New authorization is detected without relaunch"],
    },
    {
      name: "File transcription",
      userOutcome: "Transcribe a supported audio file without real-time dictation.",
      trigger: "Select an audio file from the menu bar or Settings.",
      behavior: "Validate a user-selected audio file, send the finalized file to the chosen transcription service, and route only a completed transcript through configured history and paste behavior.",
      failureOutcome: "Reject unsupported or unreadable input with no partial insertion or false history success.",
      acceptanceSignals: ["Supported formats complete", "Progress is visible", "Unsupported formats fail before upload", "No partial text is inserted"],
    },
    {
      name: "Language detection and translation",
      userOutcome: "Dictate in supported languages and optionally receive a selected output language.",
      trigger: "Enable automatic detection or select an input or output language.",
      behavior: "Apply supported language hints, automatic detection, punctuation, and optional translation through the explicitly selected provider role.",
      failureOutcome: "Preserve audio and raw text, expose uncertainty, and allow an explicit language override and retry.",
      acceptanceSignals: ["Language override is available", "Translation uses the selected target", "Uncertainty never invents a language result"],
    },
    {
      name: "Menu-bar popover and status",
      userOutcome: "Access recording, recent history, mode selection, provider status, and quick actions.",
      trigger: "Open the menu-bar item.",
      behavior: "Present an accessible keyboard-operable popover with current recording state, recent history, selected mode, provider status, and bounded recovery actions.",
      failureOutcome: "Keep offline, rate, provider, and empty states readable and actionable.",
      acceptanceSignals: ["Popover reflects live state", "Keyboard navigation works", "Reduced motion is respected", "Failure states expose bounded actions"],
    },
    {
      name: "Cost and usage visibility",
      userOutcome: "Understand provider usage and cost when the provider reports it.",
      trigger: "Complete a provider request or open provider status.",
      behavior: "Record and display provider-reported request usage and cost without estimating missing values or blocking transcription.",
      failureOutcome: "Mark usage unavailable without changing the transcript outcome.",
      acceptanceSignals: ["Reported usage is associated with one request", "Missing usage never blocks output", "No estimated charge is presented as actual"],
    },
  ],
  dataObjects: [
    { name: "Transcription history entry", purpose: "Store timestamp, source application, duration, provider, models, mode, raw text, refined text, outcome, favorite state, usage, and cost.", sensitivity: "sensitive", retentionIntent: "Keep until explicit deletion, history clearing, or the configured retention boundary." },
    { name: "Dictation mode", purpose: "Store a reusable name, instructions, output language, ordering, and application association.", sensitivity: "internal", retentionIntent: "Keep until explicitly edited, deleted, or reset." },
    { name: "Custom vocabulary entry", purpose: "Store a spoken term, preferred replacement, deterministic ordering, and modification time.", sensitivity: "personal", retentionIntent: "Keep until explicitly edited, deleted, or reset." },
    { name: "Provider configuration", purpose: "Store provider and model identifiers, endpoints, and non-secret preferences for the independent transcription and refinement roles.", sensitivity: "internal", retentionIntent: "Keep until changed or reset." },
    { name: "Hotkey and application preference", purpose: "Store hotkeys, paste behavior, per-application mode, retention, history, and login preferences.", sensitivity: "internal", retentionIntent: "Keep until changed or reset." },
    { name: "Temporary audio recording", purpose: "Hold one app-owned recording through upload and a recoverable retry decision, with an optional user-directed save action.", sensitivity: "sensitive", retentionIntent: "Delete after completion, discard, or exhausted recovery; keep a saved recording only after explicit user action." },
  ],
  externalServices: [
    { name: "Deepgram Nova streaming transcription", purpose: "Provide low-latency live speech-to-text for operations explicitly routed to Deepgram.", dataSent: ["Live microphone audio", "Selected language and model", "Supported vocabulary"], credentialRequired: true },
    { name: "OpenRouter speech-to-text", purpose: "Transcribe one finalized audio recording or selected audio file through the transcription role.", dataSent: ["Finalized audio bytes", "Audio format", "Selected language and transcription model"], credentialRequired: true },
    { name: "OpenRouter language model", purpose: "Refine an accepted raw transcript through the independently selected refinement role.", dataSent: ["Raw transcript", "Mode instructions", "Selected output language and refinement model"], credentialRequired: true },
  ],
  platformNeeds: ["audio-input", "clipboard", "global-hotkey", "accessibility-control", "notifications", "filesystem", "local-storage", "network", "background-execution", "launch-at-login"],
  qualityRequirements: [
    "Support keyboard-only operation, VoiceOver, visible focus, dark and light appearance, and reduced motion",
    "Keep recording, provider, recovery, privacy, cleanup, usage, and paste states visible",
    "Use deterministic focused tests for failure, cancellation, persistence, provider, clipboard, and packaging branches",
  ],
  productConstraints: [
    "API credentials must never appear in product records, settings files, logs, diagnostics, or generated documents",
    "Temporary audio must be deleted after completion unless the user explicitly saves a recording",
    "Never insert partial or failed text and never discard a completed transcript after insertion failure",
    "Never switch providers or incur another paid request without explicit user action",
  ],
} satisfies SemanticBlueprint
