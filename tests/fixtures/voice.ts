import type { SemanticBlueprint } from "../../src/schema"

export const nodaysidleVoiceBlueprint = {
  productName: "NODAYSIDLE Voice",
  summary: "A privacy-conscious menu-bar dictation utility that captures speech through push-to-talk or toggle recording, transcribes through an explicitly selected service, and safely inserts or preserves the result.",
  targetUsers: ["People who dictate into whichever macOS application currently has focus"],
  goals: [
    "Start and stop dictation with configurable global hotkeys",
    "Recover transcription without losing temporary audio or clipboard content",
    "Keep history, modes, vocabulary, credentials, and service choices locally controlled",
  ],
  nonGoals: ["Meeting recording or speaker diarization", "Mobile clients, hosted transcript libraries, or automatic service fallback"],
  features: [
    {
      name: "Global hotkeys and push-to-talk/toggle modes",
      userOutcome: "Start push-to-talk capture by holding a shortcut or toggle a longer recording with two presses while another application is active.",
      trigger: "Press a configured push-to-talk or start-stop shortcut.",
      behavior: "Register distinct configurable global hotkeys, reject conflicts, and keep exactly one recording mode active at a time.",
      failureOutcome: "Keep the previous working shortcut and expose the permission or conflict category.",
      acceptanceSignals: ["Push-to-talk stops on release", "Toggle mode always stops on the second press", "Conflicts never silently replace an action"],
    },
    {
      name: "Microphone capture and recording HUD",
      userOutcome: "See capture state, duration, input activity, selected mode, and a stop action while speech is recorded.",
      trigger: "A valid recording shortcut begins an authorized capture.",
      behavior: "Capture one microphone session and show a floating recording HUD until stop, cancel, interruption, or failure.",
      failureOutcome: "Missing microphone or presentation access creates no invisible capture or empty history entry.",
      acceptanceSignals: ["No capture continues after stop", "The HUD disappears after every terminal path"],
    },
    {
      name: "Provider selection",
      userOutcome: "Choose Deepgram or OpenRouter deliberately before each paid request.",
      trigger: "Select a configured transcription provider.",
      behavior: "Lock the displayed provider to the next request and never perform automatic cost-changing fallback.",
      failureOutcome: "An unconfigured provider remains unavailable and no paid request starts.",
      acceptanceSignals: ["Every request uses only the provider shown before recording", "Provider changes never rewrite history"],
    },
    {
      name: "Deepgram streaming transcription",
      userOutcome: "Receive interim text and one final transcript for the active recording.",
      trigger: "Route the active recording explicitly to Deepgram.",
      behavior: "Stream live audio, combine interim results without duplicate final text, and associate the result with exactly one recording.",
      failureOutcome: "Network or provider interruption preserves recoverable audio and exposes no raw response content.",
      acceptanceSignals: ["Interim text never duplicates final text", "One recording produces at most one accepted final transcript"],
    },
    {
      name: "OpenRouter transcription and refinement",
      userOutcome: "Transcribe finalized audio and optionally refine the transcript with the selected writing mode.",
      trigger: "Route finalized audio explicitly to OpenRouter and optionally enable refinement.",
      behavior: "Send one finalized recording to the selected transcription model and preserve raw text if optional refinement is disabled or fails.",
      failureOutcome: "Authentication, rate, transport, model, transcription, and refinement failures stay distinct and privacy-safe.",
      acceptanceSignals: ["Disabled refinement makes no refinement request", "Raw text remains available after refinement failure"],
    },
    {
      name: "Safe auto-paste and clipboard preservation",
      userOutcome: "Insert final text at the prior focus while restoring previous clipboard representations, or use a clear manual-paste fallback.",
      trigger: "A final transcript becomes ready for insertion.",
      behavior: "Snapshot the clipboard, place transcript text temporarily, paste only with accessibility authorization, and restore the snapshot after success, failure, or cancellation.",
      failureOutcome: "Insertion or restoration failure never discards the transcript or permanently hides the prior clipboard value.",
      acceptanceSignals: ["Authorized insertion targets the prior focused application", "Denied access leaves copied text and a manual path", "Clipboard restoration runs on every terminal path"],
    },
    {
      name: "Searchable transcription history",
      userOutcome: "Search, reuse, copy, and delete completed transcripts without network access.",
      trigger: "A transcription reaches a terminal result or the user opens history.",
      behavior: "Store accepted transcript text with time, provider, mode, and outcome metadata, and support local text search and explicit deletion.",
      failureOutcome: "A failed write keeps the transcript visible for immediate copy and never claims it was saved.",
      acceptanceSignals: ["Search works offline", "Deletion removes transcript text and metadata atomically"],
    },
    {
      name: "Custom modes and vocabulary",
      userOutcome: "Maintain ordered writing modes and deduplicated provider vocabulary for future requests.",
      trigger: "Create, edit, reorder, select, or delete a mode or vocabulary term.",
      behavior: "Persist valid unique names and terms, apply changes only to future requests, and expose unsupported vocabulary behavior.",
      failureOutcome: "Invalid or duplicate values remain editable without changing the saved collection.",
      acceptanceSignals: ["Duplicate terms collapse deterministically", "Deleting the active mode selects a deterministic remaining mode"],
    },
    {
      name: "Temporary audio handling",
      userOutcome: "Recover a failed request without keeping recordings longer than needed.",
      trigger: "Capture begins, transcription succeeds, the user discards, or recovery ends.",
      behavior: "Create temporary audio for one active transcription, retain it only through a recoverable retry decision, and verify cleanup after success, discard, or exhausted recovery.",
      failureOutcome: "Cleanup failure remains visible and retryable and is never reported as deleted.",
      acceptanceSignals: ["Successful and discarded requests leave no temporary audio", "Recoverable failure retains only the one required recording"],
    },
    {
      name: "Retry and provider switching",
      userOutcome: "Retry the same service or deliberately resubmit preserved audio to another configured service.",
      trigger: "A classified transcription failure leaves a recoverable recording.",
      behavior: "Create one explicit replacement request, prevent concurrent retries, and never switch providers automatically.",
      failureOutcome: "Another failure keeps a privacy-safe classification and creates no duplicate history.",
      acceptanceSignals: ["Only one active retry exists per recording", "Provider switching always requires an explicit choice"],
    },
    {
      name: "Provider and launch settings",
      userOutcome: "Configure credentials, service choices, shortcuts, and optional launch-at-login without exposing secret values.",
      trigger: "Open settings and change a provider, shortcut, credential, or login preference.",
      behavior: "Validate configuration locally, store only protected credential references, and apply login behavior only after explicit opt-in.",
      failureOutcome: "Invalid configuration leaves the prior working setting active and manual launch available.",
      acceptanceSignals: ["Stored credentials are never displayed after insertion", "Disabling login launch preserves manual launch"],
    },
  ],
  dataObjects: [
    { name: "Transcript history", purpose: "Store accepted transcript text with provider, mode, time, and outcome metadata.", sensitivity: "sensitive", retentionIntent: "Keep locally until an item is deleted or history is cleared." },
    { name: "Modes, vocabulary, provider choices, and shortcuts", purpose: "Restore explicit user configuration between launches.", sensitivity: "personal", retentionIntent: "Keep locally until changed or reset." },
    { name: "Temporary recording", purpose: "Permit retry or explicit provider switching after a recoverable failure.", sensitivity: "sensitive", retentionIntent: "Keep only through the active recovery decision, then delete and verify cleanup." },
  ],
  externalServices: [
    { name: "Deepgram", purpose: "Stream speech audio for requests explicitly routed to Deepgram.", dataSent: ["Live recording audio", "Selected language", "Supported custom vocabulary"], credentialRequired: true },
    { name: "OpenRouter", purpose: "Transcribe finalized audio and optionally refine accepted transcript text when explicitly enabled.", dataSent: ["Finalized recording audio", "Selected transcription model", "Raw transcript and selected mode only for enabled refinement"], credentialRequired: true },
  ],
  platformNeeds: ["audio-input", "global-hotkey", "accessibility-control", "clipboard", "local-storage", "network", "launch-at-login", "background-execution"],
  qualityRequirements: ["Support keyboard-only settings and visible focus.", "Respect reduced motion.", "Expose recording, provider, recovery, privacy, and cleanup states in text."],
  productConstraints: ["Only one recording and one provider request may be active.", "Never erase recoverable audio on network loss.", "Never log credentials, raw provider bodies, transcript content, or audio content."],
} satisfies SemanticBlueprint
